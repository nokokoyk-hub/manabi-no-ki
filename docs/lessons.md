# lessons.md — ちゃぴの経験メモ（まなびの木）

> **これは地図であって檻やない。** 過去の経験則をまとめたもので、ちゃぴの行動範囲を狭めるためのものではない。
> 現状とメモが食い違うときは、盲目的に従わず「メモと状況が違うけどどうする？」とのんに確認すること。
> 各項目には日付・理由（出典）を必ず付ける。理由のない禁止は書かない。

## 🔒 絶対ルール（明示的に解除されるまで守る）

- 2026-07-22：認証・決済・DBスキーマ・サーバー側関数・本番導線は明示指示なしで触らない（共通CLAUDE.md由来）
- 2026-07-22：TWA判定にlocalStorage使用禁止（Chromeと共有され事故る）→ sessionStorage。TWA内に外部決済へのタップ可能リンクを置かない（current_state.md v1.0.6由来）
- 2026-07-22：日付処理で toISOString / toLocaleDateString 禁止 → toSafeDateStr() パターン（current_state.md由来）

## 🧭 原則（基本従う。状況が変わっていたら再検討を提案）

- ⭕ 2026-07-22：**Vercelプレビューの動作確認はメールOTP（8桁コード）ログインを使う**。GoogleログインはSupabaseのリダイレクト許可リスト外のプレビューURLから本番へ着地し、「プレビューに新機能が出ない」ように見える罠がある（本日実際に発生・原因特定済み）
- ⭕ 2026-07-22：まとまった実装は /implement パイプライン（実装→テスト→レビュー）で回す。本日2機能とも致命バグゼロでリリースでき、テスト工程が既存の隠れバグ（🐕絵文字固定・collection解放遅延）まで拾った
- ❌ 2026-07-22：新しい解放条件・実績タイプを追加するときは「**判定がどのタイミングで走るか**」を必ず確認する。collection条件を足したらミッション時のみ判定で、収穫直後に反映されない非対称が出た（同日修正済み）
- ⭕ 2026-07-22：マージ済みブランチで続きの作業をするときは `git fetch origin main && git checkout -B <branch> origin/main` で仕切り直してから積む（マージ済み履歴に新コミットを重ねない）
- ⭕ 2026-07-22：将来拡張の「受け入れ口」をコメント付きで残しておくと後日の実装が軽い。CharacterDisplay.jsの拡張ポイント予告コメントのおかげで、ガチャキャラせんせい機能が予定調和で実装できた
- ⭕ 2026-07-22：拡張されうるデータ（アイテム・キャラ等）は「定義ファイルに1行追加で完結」するデータ駆動に寄せる。UI・判定は定義から自動追従させ、手打ちの同期箇所（例：リセット値のベタ書き）を作らない

## 💛 好み（のんの希望。柔らかく尊重）

- 2026-07-22：画像素材はのんがChatGPTで自作する。**仕様（サイズ・正方形・構図の注意）を先に具体的に渡す**とスムーズ。背景処理（flood-fill透過）はちゃぴ担当
- 2026-07-22：新機能はプレビューで確認→「マージして」の一言で本番反映、の流れが好み。マージ・デプロイ操作はちゃぴが実行して結果を報告する
