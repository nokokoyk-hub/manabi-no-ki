# 作業ダイジェスト 2026-07-22（JST）

## 🎉 ビッグニュース

- **Google Play 審査通過！**（7/11再申請分が承認。v1.0.12の起動安全網が効いた）

## 今日やったこと

### ① ガチャキャラせんせい機能（v1.0.13・PR #20・本番反映済み）
- ガチャキャラ6体（にじぴよ・ぽっけ・ももぴ・ガーディ・ライドラ・ひめにゃ）を「せんせい」（出題キャラ）として選択可能に。**コレクション入手済みのみ選択可**
- 新規 `src/components/GachaCharacter.js`（立ち絵+吹き出し、画像失敗時emojiフォールバック）
- `CharacterDisplay.js` の予告済み拡張ポイントに分岐実装 / `gachaData.js` に `GACHA_CHARACTERS`・`isGachaCharacter()` 追加
- `HomeScreen.js` に「🎓 せんせいを えらぶ」ボタン+選択モーダル（未入手は❓表示）。ガチャキャラ選択中は木の右に表示
- `mameMessages.js` にガチャキャラ用汎用セリフ / `App.js` に未入手・不明IDの'mame'フォールバック
- `LearningScreen.js` の直接分岐をCharacterDisplay経由に統一+結果画面🐕絵文字のキャラ出し分け（既存の隠れバグ修正）

### ② 着せ替え豪華版（v1.0.14・PR #21・本番反映済み）
- データ構造変更：`equippedItem`（単数）→ `equippedItems`（head/face/neck/hand の4スロット）で**重ねづけ対応**。旧形式localStorageからの自動マイグレーション付き
- アイテム8個→**16個**（新カテゴリ「て」、新unlockType `collection`=果実コレクション種類数）
- `costumeItems.js` に任意 `image` フィールド（将来のイラスト差し替え口、image→emojiフォールバック）
- GohoubiScreen をカテゴリ別セクションUIに / collection型解放は**収穫直後に即反映**（lamchan指摘対応）
- レビュー対応：リセット値を `createDefaultCostumeData()` に集約（App.jsの手打ちスロット・item_none遺物を除去）

## 決めたこと

- ガチャキャラは1枚絵のため「立ち絵+吹き出し」方式（ポーズ絵はのんが今後GPTで作成予定：正方形・1キャラ3ポーズ案）
- 新規8アイテムの内訳はzundamon裁量で調整（メダル🏅案は割愛して計16個に）。ロック中表示は「？？？」に変更
- 着せ替えデータはlocalStorageのまま（Supabase移行は別タスクとして継続）
- マージ済みブランチの続きは origin/main から `checkout -B` で仕切り直す運用

## 見送ったこと・未解決

- 松プラン（着せ替えイラスト画像化）→ のんの画像待ち。受け入れ口は実装済み
- パズル種類追加 → のんの画像待ち（正方形1024×1024）
- レビュアー宿題：SpeechBubble共通化（3ファイル重複）/ HomeScreen分割（708行）/ GohoubiScreen分割（417行）
- LevelSettingsScreen に selectedCharacter 未伝播（既存の抜け・まめ固定表示）
- 成長演出セリフのガチャキャラ専用トーン（現状まめ用汎用文でフォールバック・実害なし）

## 発生した問題と解決

- **「プレビューにせんせいボタンが出ない」問題**：原因はGoogleログインの認証リダイレクト。プレビューURL（vercel.app）はSupabaseのリダイレクト許可リスト外のため、ログイン後に本番 manabinoki.net へ着地していた。**プレビュー確認はメールOTP（8桁コード）ログインを使えば回避できる**（確認済み）
- collection型の着せ替え解放が「次のミッションクリアまで反映されない」非対称 → handleHarvestClose に checkCostumeUnlocks 呼び出しを追加して即時反映

## version判定

- v1.0.13 / v1.0.14 の2回バンプ（各機能リリース時に4箇所同期済み）
- 本wrapupのドキュメント更新はバンプ不要と判定
